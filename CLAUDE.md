# potato

git / GitHub / Claude Code 사용법을 정리해 두는 개인 문서 저장소.

---

## 이 파일이 하는 일

Claude Code는 작업 폴더 루트의 `CLAUDE.md`를 **세션 시작 시 자동으로 읽는다.**
따라서 "매번 지켜야 할 규칙"은 반드시 이 파일에 적어야 실제로 적용된다.

- `커밋_컨벤션.txt` 같은 일반 txt 파일은 **사람이 읽는 문서일 뿐** 자동으로 반영되지 않는다.
- 이 파일은 git으로 함께 관리되므로, 다른 노트북에서 `clone` 하면 규칙도 그대로 따라온다.
- 전체 프로젝트 공통 규칙은 `C:\Users\user\.claude\CLAUDE.md`에 두지만,
  그건 해당 PC에만 있으므로 기기를 옮기면 따라오지 않는다. 저장소 안에 두는 쪽이 낫다.

---

## 커밋 규칙

Conventional Commits 형식을 따른다.

```
<type>: <제목>

<본문>
```

- **type**: `feat` / `fix` / `docs` / `style` / `refactor` / `test` / `chore`
- **제목**: 명령형, 50자 이내, 마침표 없음
- **본문**: 제목과 빈 줄로 구분. "무엇을"보다 **"왜"** 중심으로 작성. 짧은 변경이면 생략 가능
- 커밋 전 `git diff`로 실제 변경분을 확인한다

예시:

```
docs: 새 노트북 세팅 체크리스트 추가

노트북을 옮길 때마다 절차를 다시 찾아보게 되어 문서로 고정함.
```

---

## 브랜치 규칙

이 저장소는 혼자 쓰는 개인 문서 저장소이므로 **`main`에 직접 커밋한다.** 브랜치를 따로 파지 않는다.

- 회사 PC와 집 노트북을 오가며 쓰지만 동시에 작업하지 않으므로, 브랜치로 작업을 분리할 실익이 없다.
- 나중에 실제 코드 저장소(예: DSD 자동화 스크립트)를 만들 때는 그 저장소의 `CLAUDE.md`에 브랜치 규칙을 둔다. 규칙은 저장소마다 독립적이라 서로 영향이 없다.

---

## 동기화 규칙 — 이 저장소에서 가장 중요

회사 PC ↔ 집 노트북을 오가므로 순서를 지킨다.

- **작업 시작 전** → `git pull`
- **작업 완료 후** → 커밋하고 `git push`

이유:

- 커밋만 하고 push 하지 않으면 다른 기기에서 받을 수 없다. 커밋은 로컬에만 남는 기록이다.
- 커밋하지 않은 수정사항은 아예 넘어가지 않는다.
- pull 없이 작업을 시작하면 양쪽에 서로 다른 커밋이 생겨 `diverged` 상태가 된다.

Claude는 이 저장소에서 작업을 시작할 때 `git status`로 원격과의 차이를 먼저 확인하고, `behind` 상태면 알린다.

---

## 푸시 규칙

- **"커밋해줘"** → 커밋까지만 하고 멈춘다
- **"푸시해줘"** → 원격에 올린다
- 커밋 후에는 현재 브랜치가 `origin`보다 몇 커밋 앞서 있는지 보고한다

푸시는 원격에 반영되면 되돌리기가 번거로우므로 별도 지시를 받는다.

---

## 이 저장소 작업 시 주의

- 이 저장소는 **public**이다. 회사 자료, 계정 정보, 거래처명, API 키가 들어가지 않도록 주의한다.
- 경로에 한글이 포함되어 있어(`Desktop\정성호\개인\github\potato`) PowerShell에서 `cd`나
  `git -C`가 실패하는 경우가 있다. 명령을 `;`로 이어 붙이지 말고, `git init` 계열 명령은
  실행 후 출력된 경로를 반드시 확인한다.

---

## 문서 목록

| 파일 | 내용 |
|---|---|
| `git_기본개념.txt` | origin, clone, push/pull, `origin/main` 표기의 의미 |
| `새노트북_세팅_체크리스트.txt` | clone → gh 설치 → 로그인 절차 |
| `커밋_컨벤션.txt` | 커밋 메시지·브랜치 규칙 상세 (이 파일의 원본 문서) |
| `github_세팅_및_claude_활용법.txt` | gh CLI 상세, Claude에게 요청하는 문구 예시 |
| `claude_code_실행계획서_워크플로우.txt` | 높은 모델로 계획 작성 → 낮은 모델로 실행 |
